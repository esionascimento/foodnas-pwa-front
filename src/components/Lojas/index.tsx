import { useState } from "react";

const LojasComponent = () => {
  return (
    <>
      <div>
        <p>Lojas</p>
        {/* <ul>
          {data?.lessons.map(lesson => {
            return <li key={lesson.id}>{lesson.title}</li>
          })}
        </ul> */}
      </div>
    </>
  )
}

export default LojasComponent;
